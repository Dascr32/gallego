<div class="row">
  <div class="col-md-12">
     <div class="page-header">
       <h2>Clase de Red</h2>
     </div>
   </div>

   <div class="col-md-12">
     <h3>Instrucciones</h3>
     <p class="text-justify">
       En esta seccion Gallego podra identificar a que clase pertenece su red (A o B) comparandola con
       otras y encontrando la que mas se le parezca. Solo debe completar el formulario de abajo.
     </p>
     <hr>
   </div>

   <div class="col-md-8 col-md-offset-2">
    <form class="form" id="netForm">
      <div class="panel panel-default">

        <div class="panel-body">
          <div class="form-group">
            <label for="campusSelectGender">Confiabilidad de la red</label>
            <select class="form-control" id="netReliability">
              <% (2..5).each do |rating| %>
                <option value="<%= rating %>"><%= rating %></option>
              <% end %>
            </select>
          </div>

          <div class="form-group">
            <label for="campusSelectGender">Numero de enlaces</label>
            <select class="form-control" id="netLinks">
              <% (7..20).each do |rating| %>
                <option value="<%= rating %>"><%= rating %></option>
              <% end %>
            </select>
          </div>

          <div class="col-md-6">
            <h5><strong>Capacidad</strong></h5>
            <label class="radio-inline"><input type="radio" name="netCapacityRadio" value="low" checked>Baja</label>
            <label class="radio-inline"><input type="radio" name="netCapacityRadio" value="medium">Media</label>
            <label class="radio-inline"><input type="radio" name="netCapacityRadio" value="high">Alta</label>
          </div>

          <div class="col-md-6">
            <h5><strong>Costo</strong></h5>
            <label class="radio-inline"><input type="radio" name="netCostRadio" value="low" checked>Bajo</label>
            <label class="radio-inline"><input type="radio" name="netCostRadio" value="medium">Medio</label>
            <label class="radio-inline"><input type="radio" name="netCostRadio" value="high">Alto</label>
          </div>

          <button type="button" class="btn btn-default btn-sm" id="guessNetBtn">Averiguar</button>
        </div>

      </div>
    </form>
  </div>

  <div class="col-md-12" id="netSuccess">
     <hr>
     <h3>Su red es de clase <span class="label label-success" id="netResult"> - </span> </h3>
     <p id="netComputingMsg">Computing...</p>
  </div>
</div>
